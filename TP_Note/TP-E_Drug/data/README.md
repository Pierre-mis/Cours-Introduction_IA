# Données — TP-E

Place ici le fichier **`drugsComTrain_raw.csv`** (ou équivalent) du dataset *UCI ML Drug Review* :
https://www.kaggle.com/datasets/jessicali9530/kuc-hackathon-winter-2018

Le starter notebook attend `data/drugsComTrain_raw.csv`. Si ton fichier a un autre nom (ex : `drugsCom_raw.tsv`), adapte le `pd.read_csv` au début du notebook.
